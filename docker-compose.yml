services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
    volumes:
      - runacos-data:/app/prisma
    restart: unless-stopped

volumes:
  runacos-data:
